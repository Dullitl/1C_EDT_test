//Сторчевой А.Н. НФС 2018 {
Процедура ВыполнитьЗаписьПоРезультатуЗапроса(РезультатЗапроса, ДоговорКонтрагента) Экспорт 
//ххх Брель Виктор Андреевич 25.03.2018 15:05:16, заявка <<<

Если НЕ РезультатЗапроса.Пустой() Тогда
	Выборка		= РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Запись = РегистрыСведений.абс_ПроектыРезервовРНПДПоЗЗ.СоздатьМенеджерЗаписи();	
		
		Если Выборка.Остаток > 0 Тогда
			
			ЗаполнитьЗначенияСвойств(Запись, Выборка);
			Запись.Период								= Выборка.Период;
			Запись.ДоговорКонтрагента					= ДоговорКонтрагента;
			Запись.СуммаРНП	   = Выборка.Остаток;
			Запись.СуммаНДС = УчетНДС.РассчитатьСуммуНДС(Запись.СуммаРНП,
	                                                   Истина, Ложь,
	                                                   УчетНДС.ПолучитьСтавкуНДС(Выборка.СтавкаНДС));
			Запись.ttk_ПериодВозникновенияЗадолженности = Выборка.Период;			
		КонецЕсли;
		Если Запись	<> Неопределено Тогда
			Запись.Записать();
		КонецЕсли;
		
	КонецЦикла;
КонецЕсли;

// Брель Виктор Андреевич 25.03.2018 15:05:16 >>>
	
	
КонецПроцедуры
//} Сторчевой А.Н. НФС 2018