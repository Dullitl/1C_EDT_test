
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ОтборДокумент = Неопределено;
	Если Параметры.Свойство("Документ") Тогда 
		ОтборДокумент = Параметры.Документ;
	КонецЕсли;
	
	Список.Параметры.УстановитьЗначениеПараметра("Документ", ОтборДокумент);
	
КонецПроцедуры
