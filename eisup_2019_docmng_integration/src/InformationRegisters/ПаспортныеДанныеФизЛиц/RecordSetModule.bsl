
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;	
	
	Для Каждого ЗаписьСтрока Из ЭтотОбъект Цикл 
		ЗаписьСтрока.ДокументНомер = СтрЗаменить(СтрЗаменить(СокрЛП(ЗаписьСтрока.ДокументНомер),Символы.НПП,"")," ","")
	КонецЦикла;
	           	
КонецПроцедуры
