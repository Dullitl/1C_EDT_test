
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//Вставить содержимое обработчика
	Если Параметры.Свойство("Контрагент") Тогда
		
		парКонтрагент = Параметры.Контрагент;
		
		ПараметрКонтрагент = Список.Параметры.Элементы[0];
		
		ПараметрКонтрагент.Значение = парКонтрагент;
		ПараметрКонтрагент.Использование = Истина;
		//ПараметрДоговор.Параметр = Новый ПараметрКомпоновкиДанных("ДоговорКонтрагента");
		
	КонецЕсли;
	
КонецПроцедуры
