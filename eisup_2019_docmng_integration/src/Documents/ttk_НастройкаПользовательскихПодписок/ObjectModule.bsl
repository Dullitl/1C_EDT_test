
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	Если ЭтоНовый() Тогда
		ВыводитьСписком = Ложь;
		Подписки.Добавить();
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	
	Движения.ttk_ПользовательскиеПодпискиНаСобытия.Записывать = Истина;
	Движения.ttk_ПользовательскиеПодпискиНаСобытия.Очистить();
	Для Каждого ТекСтрокаПодписки Из Подписки Цикл
		Движение = Движения.ttk_ПользовательскиеПодпискиНаСобытия.Добавить();
		Движение.ВидПодписки = ВидПодписки;
		Движение.Организация = Организация;
		Движение.Пользователь = ТекСтрокаПодписки.Пользователь;
		Движение.ПодразделениеОрганизации = ТекСтрокаПодписки.ПодразделениеОрганизации;		
	КонецЦикла;
	
КонецПроцедуры
