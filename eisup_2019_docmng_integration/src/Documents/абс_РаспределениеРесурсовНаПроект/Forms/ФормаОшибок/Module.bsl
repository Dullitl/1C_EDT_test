
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("МассивОшибок") Тогда 
		Для  Каждого строка из Параметры.МассивОшибок Цикл 
			стркоаОшибки = ОшибкиРаспредления.Добавить();
			стркоаОшибки.ОписаниеОшибки = строка;
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры


