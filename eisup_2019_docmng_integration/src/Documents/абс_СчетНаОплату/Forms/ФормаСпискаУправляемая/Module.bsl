
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//Вставить содержимое обработчика
	
	Если Параметры.Свойство("ДоговорКонтрагента") Тогда
		Если ЗначениеЗаполнено(Параметры.ДоговорКонтрагента) Тогда
			ОтборСтатус = Список.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборСтатус.ВидСравнения 	= ВидСравненияКомпоновкиДанных.Равно;
			ОтборСтатус.ЛевоеЗначение 	= Новый ПолеКомпоновкиДанных("ДоговорКонтрагента");
			ОтборСтатус.ПравоеЗначение 	= Параметры.ДоговорКонтрагента;
			ОтборСтатус.Использование	= Истина;
			ОтборСтатус.РежимОтображения= РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Недоступный;
		КонецЕсли;
	КонецЕсли;
	
	Если Параметры.Свойство("Контрагент") Тогда
		Если ЗначениеЗаполнено(Параметры.Контрагент) Тогда
			ОтборСтатус = Список.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборСтатус.ВидСравнения 	= ВидСравненияКомпоновкиДанных.Равно;
			ОтборСтатус.ЛевоеЗначение 	= Новый ПолеКомпоновкиДанных("Контрагент");
			ОтборСтатус.ПравоеЗначение 	= Параметры.Контрагент;
			ОтборСтатус.Использование	= Истина;
			ОтборСтатус.РежимОтображения= РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Недоступный;
		КонецЕсли;
	КонецЕсли;	
	
	Если Параметры.Свойство("ЗакупочныйЗаказ") Тогда
		Если ЗначениеЗаполнено(Параметры.ЗакупочныйЗаказ) Тогда

			ОтборЗаказ = Список.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЗаказ.ВидСравнения 		= ВидСравненияКомпоновкиДанных.Равно;
			ОтборЗаказ.ЛевоеЗначение		= Новый ПолеКомпоновкиДанных("ЗакупочныйЗаказ");
			ОтборЗаказ.ПравоеЗначение		= Параметры.ЗакупочныйЗаказ;
			ОтборЗаказ.Использование		= Истина;
			ОтборЗаказ.РежимОтображения		= РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Недоступный;
		КонецЕсли;	
	КонецЕсли;
	
	КонецПроцедуры
