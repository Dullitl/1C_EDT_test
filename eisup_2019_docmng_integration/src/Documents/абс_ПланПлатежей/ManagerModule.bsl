

Функция ПолучитьСтруктуруПериода(ТекНомерНедели) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст ="ВЫБРАТЬ
	|	НАЧАЛОПЕРИОДА(РегламентированныйПроизводственныйКалендарь.ДатаКалендаря, НЕДЕЛЯ) КАК ДатаНачала,
	|	КОНЕЦПЕРИОДА(РегламентированныйПроизводственныйКалендарь.ДатаКалендаря, НЕДЕЛЯ) КАК ДатаКонца
	|ИЗ
	|	РегистрСведений.РегламентированныйПроизводственныйКалендарь КАК РегламентированныйПроизводственныйКалендарь
	|ГДЕ
	|	НЕДЕЛЯ(РегламентированныйПроизводственныйКалендарь.ДатаКалендаря) = &НомерНедели";
	Запрос.УстановитьПараметр("НомерНедели",ТекНомерНедели);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		ТекДатаНачала = Выборка.ДатаНачала;
		ТекДатаКонца = Выборка.ДатаКонца;
	Иначе
		ТекДатаНачала = '00010101';
		ТекДатаКонца =  '00010101';
	КонецЕсли;
	
	СтруктураПериода = Новый Структура();
	
	СтруктураПериода.Вставить("ДатаНач", ТекДатаНачала);
	СтруктураПериода.Вставить("ДатаКон", ТекДатаКонца);
	
    Возврат СтруктураПериода;
	
КонецФункции
