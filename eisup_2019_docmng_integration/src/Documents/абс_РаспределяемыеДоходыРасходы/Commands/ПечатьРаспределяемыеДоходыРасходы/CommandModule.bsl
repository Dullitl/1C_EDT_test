
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если ТипЗнч(ПараметрКоманды) = Тип("Массив") И ПараметрКоманды.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого Строка Из ПараметрКоманды Цикл 	
		ТабДок = Документы.абс_РаспределяемыеДоходыРасходы.ВыполнитьПечатьРаспределяемыеДоходыРасходы(Строка.ПолучитьОбъект());
		УниверсальныеМеханизмы.НапечататьДокумент(ТабДок, 1, Ложь,"Распределяемые доходы/расходы");  
	КонецЦикла;

КонецПроцедуры
