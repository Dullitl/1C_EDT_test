Функция  ПолучитьТекущийСтатусЗаявкиНаОтгрузку(Ссылка) Экспорт
	
	Запрос = новый Запрос;


	Запрос.Текст = 	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	               	|	ttk_ИзменениеСтатусовЗаявокНаОтгрузкуСрезПоследних.Статус
	               	|ИЗ
	               	|	РегистрСведений.ttk_ИзменениеСтатусовЗаявокНаОтгрузку.СрезПоследних(, ЗаявкаНаОтгрузку = &ЗаявкаНаОтгрузку) КАК ttk_ИзменениеСтатусовЗаявокНаОтгрузкуСрезПоследних";


	Запрос.УстановитьПараметр("ЗаявкаНаОтгрузку",Ссылка);

	Выборка =ЗАпрос.Выполнить().Выбрать();


	Если Выборка.Следующий() Тогда
		
		Возврат Выборка.Статус;
		
	Иначе
		
		Возврат Перечисления.ttk_СтатусыЗаявокНаОтгрузку.ПустаяСсылка();
		
	КонецЕсли;
	
	
КонецФункции
