
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	Если ЗначениеЗаполнено(ПараметрКоманды) Тогда
		ПараметрыФормы = ПолучитьПараметрыФормы(ПараметрКоманды);		
	Иначе
		
		ttk_ОбщегоНазначения.СообщитьОбОшибке("Закупочный заказ не записан.");
		Возврат;
	КонецЕсли;
	
	Если ПараметрыФормы = Неопределено Тогда 
		Возврат;
	КонецЕсли;
	
	ОткрытьФорму("Отчет.абс_ОстаткиЛимитовПоБюджетуСКД.Форма", ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно);
	
КонецПроцедуры


&НаСервере
Функция ПолучитьПараметрыФормы(Ссылка) Экспорт
		
	ПараметрыФормы = Новый Структура();
	
	ПараметрыФормы.Вставить("ЦФО"				, Ссылка.ЦФО);
	ПараметрыФормы.Вставить("ЦФУ"				, Ссылка.ЦФУ);
	ПараметрыФормы.Вставить("Организация"		, Ссылка.Организация);
	ПараметрыФормы.Вставить("СтатьяОборотов"	, Ссылка.БюджетнаяСтатья);	
	
	Возврат ПараметрыФормы;
	
КонецФункции
