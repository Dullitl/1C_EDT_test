
Функция Добавить(PackXDTO)
	
	ОтветXDTO = ФабрикаXDTO.Создать(ФабрикаXDTO.Тип("http://www.abs-softNom.ru/", "Answer"));
	ОтветXDTO.ID 	= "";
	//ОтветXDTO.Error = "";
	
	ОбработчикWebService = абс_WebService.ПолучитьОбработчикWebService();
	
	Попытка 
		// Вызов обработчика
		ОтветXDTO = ОбработчикWebService.СоздатьНоменклатуру(PackXDTO);
	Исключение
		ОписаниеОшибки = ОписаниеОшибки();
		абс_WebService.СообщитьОбОшибке(ОписаниеОшибки, "Вызов обработчика согласования создания номенклатуры по вебсервису");
		ОтветXDTO.Error = ОписаниеОшибки;
	КонецПопытки;
	
	Возврат ОтветXDTO;

КонецФункции
Функция ИзменитьСтатусНоменклатуры(PackXDTO)
	
	ОтветXDTO = ФабрикаXDTO.Создать(ФабрикаXDTO.Тип("http://www.abs-softNom.ru/", "Answer"));
	ОтветXDTO.ID 	= "";
	//ОтветXDTO.Error = "";
	
	ОбработчикWebService = абс_WebService.ПолучитьОбработчикWebService();
	
	Попытка 
		// Вызов обработчика
		ОтветXDTO = ОбработчикWebService.ИзменитьСтатусНоменклатуры(PackXDTO);
	Исключение
		ОписаниеОшибки = ОписаниеОшибки();
		абс_WebService.СообщитьОбОшибке(ОписаниеОшибки, "Вызов обработчика согласования создания номенклатуры по вебсервису");
		ОтветXDTO.Error = ОписаниеОшибки;
	КонецПопытки;
	
	Возврат ОтветXDTO;

КонецФункции

Функция ТестСоединения()
	ОтветXDTO = ФабрикаXDTO.Создать(ФабрикаXDTO.Тип("http://www.abs-softNom.ru/", "Answer"));
	ОбработчикWebService = абс_WebService.ПолучитьОбработчикWebService();
	ОтветXDTO.ID 	= Строка(ТипЗнч(ОбработчикWebService)) + " Соединение успешно выполнено!";
	//ОтветXDTO.Error = "";
	
	Возврат ОтветXDTO;
КонецФункции

