
////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ - ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//Если Не ЗначениеЗаполнено(Объект.Ссылка) Тогда
	//	
	//	ТекПользователь = глЗначениеПеременной("глТекущийПользователь");
	//	ОсновнаяОрганизация = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(ТекПользователь, "ОсновнаяОрганизация");
	//	МассивРолей =абс_УправлениеРолямиИПользователями.ПолучитьСписокДоступныхРолейПользователя(ТекПользователь, ?(ЗначениеЗаполнено(ОсновнаяОрганизация), ОсновнаяОрганизация, Неопределено));
	//	
	//	Если МассивРолей.Найти(Справочники.РолиИсполнителей.абс_СотрудникиНСИ) = Неопределено Тогда
	//		Сообщить("У пользователя недостаточно прав для создания элемента справочника");
	//		Отказ = Истина;
	//		Возврат;
	//	КонецЕсли;	
	//	
	//КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	//МассивРолей =абс_УправлениеРолямиИПользователями.ПолучитьСписокДоступныхРолейПользователя(глЗначениеПеременной("глТекущийПользователь"));

	//Если МассивРолей.Найти(Справочники.РолиИсполнителей.абс_СотрудникиНСИ) = Неопределено Тогда
	//
	//	ТолькоПросмотр = Истина;
	//	Сообщить("Редактирование элемента справочника запрещено!");
	//	
	//КонецЕсли;

КонецПроцедуры

