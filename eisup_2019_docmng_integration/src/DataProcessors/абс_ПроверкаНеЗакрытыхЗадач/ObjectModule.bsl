
Процедура ЗаписьВРегистр(ОтложитьНаВремя) Экспорт
	
	Если ЗначениеЗаполнено(ОтложитьНаВремя) Тогда
		
		ДатаКонтроля = ТекущаяДата();
		Пользователь = глЗначениеПеременной("глТекущийПользователь");
				
		Если ОтложитьНаВремя = "Один час" Тогда
			ОтложитьНаВремя = 3600;
		ИначеЕсли ОтложитьНаВремя = "Два часа" Тогда
			ОтложитьНаВремя = 7200;
		ИначеЕсли ОтложитьНаВремя = "30 мин." Тогда
			ОтложитьНаВремя = 1800;				 
		Иначе
			ОтложитьНаВремя = 0; 
		КонецЕсли;	  
				
		НаборЗаписей = РегистрыСведений.абс_ОтложенныеЗадачиПоПроверкиЗаяков.СоздатьНаборЗаписей();
			
		НаборЗаписей.Отбор.Пользователь.Установить(Пользователь);
		НаборЗаписей.Отбор.Период.Установить(ДатаКонтроля);
			
		НовЗапись = НаборЗаписей.Добавить();

		НовЗапись.Пользователь = Пользователь;
		НовЗапись.Период       = ДатаКонтроля;
		НовЗапись.ОтложенноВСекундах = ТекущаяДата() + ОтложитьНаВремя;

		НаборЗаписей.Записать(Истина);
		
		
	КонецЕсли;
	
КонецПроцедуры	
